#!/usr/bin/env ruby
# vim: ft=ruby

require "rubygems"
require "thor"

# temporarily do this while we're in the glue stage
$LOAD_PATH.unshift File.expand_path("../../lib", __FILE__)

require "vmc"
require "vmc/plugin"

VMC::Plugin.load_all

$exit_status = 0
VMC::CLI.start(ARGV)
exit($exit_status)
